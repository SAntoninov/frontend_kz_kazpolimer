import { PhysicalProperties } from './physical-properties';

describe('PhysicalProperties', () => {
  it('should create an instance', () => {
    expect(new PhysicalProperties()).toBeTruthy();
  });
});
